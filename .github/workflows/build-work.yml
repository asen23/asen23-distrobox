name: build-work
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
    paths:
      - 'Containerfile.work'
      - 'packages.work'

jobs:
  build-image:
    name: Build Images
    uses: ./.github/workflows/reusable-build.yml
    secrets: inherit
    with:
      base_name: work
